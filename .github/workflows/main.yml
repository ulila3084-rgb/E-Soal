name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    # Memberikan izin kepada sistem untuk membuat rilis
    permissions:
      contents: write 

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: '9.3.1'
        cache-disabled: true

    - name: Generate Debug Keystore
      run: |
        keytool -genkey -v -keystore debug.keystore -alias androiddebugkey -storepass android -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"

    - name: Build APK
      env:
        _JAVA_OPTIONS: "-Djava.awt.headless=true"
      run: |
        gradle clean assembleDebug \
          -Pkotlin.compiler.execution.strategy=in-process \
          -Dorg.gradle.jvmargs="-Xmx3200m -Djava.awt.headless=true -XX:MaxMetaspaceSize=1024m" \
          --no-daemon \
          --no-configuration-cache

    # PENGGANTI ARTIFACT: Mengirim APK ke halaman GitHub Releases
    - name: Upload APK to Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: APK-Terbaru-${{ github.run_number }}
        name: APK Build ${{ github.run_number }}
        files: app/build/outputs/apk/debug/app-debug.apk
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
